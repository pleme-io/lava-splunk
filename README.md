# lava-splunk

Autogenerated typed `(deflava-resource …)` shapes for every splunk
terraform-provider resource. Mirrors pangea-splunk.

## Regenerate

```bash
tofu providers schema -json > schema.json
lava-forge generate --schema schema.json --out resources
```
